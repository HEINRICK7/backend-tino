# Architecture entrypoint

The authoritative SDD starts at [`docs/README.md`](docs/README.md). Architecture decisions, system specifications, milestone gates, permanent invariants, and objective evidence are kept separately so implementation cannot silently redefine the baseline.
