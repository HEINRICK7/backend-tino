package com.tino.backend.bootstrap.domain.model;

/** The only three product states in the M5 bootstrap contract. */
public enum BootstrapState {
    BUSINESS_REQUIRED,
    LOCAL_BUSINESS_LINK_REQUIRED,
    READY
}
